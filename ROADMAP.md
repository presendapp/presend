# ROADMAP Presend (mis à jour le 26 septembre 2026)

## Fait récemment (session du 25 sept.)
- [x] 4 nouveaux endpoints créés, testés, déployés : iban-validate, link-metadata, vat-validate, cve-lookup
- [x] Exposés côté MCP (41 outils au total désormais)
- [x] Compteurs obsolètes ("42 endpoints") corrigés partout (site, README, OpenAPI, profil GitHub) — vrai chiffre : 48
- [x] 4ᵉ fiche RapidAPI (Developer Tools) ajoutée à la doc /api, qui manquait
- [x] Collection Postman réellement resynchronisée via l'API (pas juste le chiffre affiché)
- [x] Page de statut BetterStack découverte (déjà active) et intégrée au site
- [x] Correction Spamhaus propagée (13 issues GitHub + site + OpenAPI)
- [x] Bug de plateforme RapidAPI (corps JSON supprimé) diagnostiqué, signalé, corrigé sur les 4 fiches
- [x] Documentation projet (ce fichier + PROJECT_CONTEXT.md + STRUCTURE.md + WORKFLOW.md) entièrement réécrite après un premier jet incomplet/inexact

## Fait le 26 sept.
- [x] session-check : pas de réponse de tripwire #143 (2) ni de guard-core #119 (1) ; fastapi-guard #137 stable (5). Toujours en attente de leurs réponses aux 4 questions.
- [x] IntelOwl : le PR d'AnshSinghal est #4037 (#4035 fermée, doublon), review demandée à mlodic. Mock de test vérifié identique à la réponse de prod ; suggestion de `timeout=10` postée (https://github.com/intelowlproject/IntelOwl/pull/4037#issuecomment-5844217081).
- [x] **MCP v3.1.0** : annotations sur les 40 outils (`readOnlyHint: true`, `destructiveHint: false`, `idempotentHint: true` ; `openWorldHint: false` pour les 15 outils de calcul local de `CLOSED_WORLD`, classés d'après le code : aucun `fetch`/`validateAndResolve`/`connect` dans l'endpoint ni ses imports). Vérifié en local puis en prod.
- [x] `address_risk` : la description du paramètre laissait croire que les adresses bech32 étaient vérifiées (l'API renvoie `sanctioned: null`). Corrigé dans openapi.json, openapi-blockchain.json, mcp.js et les 2 exports RapidAPI.
- [x] Registre MCP officiel republié en **3.1.0** (il affichait 36 outils / v1.0.1, et non 41 comme noté le 25 sept.). `server.json` du repo = source unique ; doublon `github-contributions/presend-mcp-registry/` supprimé.
- [x] unjs/ipx #336 : fermée sans merge par le mainteneur le 25 sept. (voir SECURITY_DISCLOSURES.md).
- [x] **typosquat-check** : normalisation PyPI PEP 503 (`typing_extensions` était signalé), règle du même scope npm (`@aws-sdk/client-sso` vs `client-s3`), `KNOWN_LEGIT` (47 PyPI + 57 npm revus un par un, champ additif `known_legitimate`), +16 cibles npm / +14 PyPI (electron, discord.js, ethers, discord.py, pycryptodome...). Faux positifs sur paquets populaires : top 15 000 PyPI 58 -> 11, npm-high-impact 65 -> 1, tous voulus ; 27/27 typosquats détectés. Tests versionnés : `tests/typosquat/`. Vérifié en local puis en prod.
- [x] **maintainer-change-check** : un nouveau publieur qui maintient déjà un AUTRE paquet npm à 100 000+ téléchargements hebdomadaires passe dans `established_publisher_events` (preuve incluse) au lieu de déclencher `suspicious`. Top 200 npm-high-impact évalué à 9 dates sur 2 ans : 13 paquets signalés -> 1 (`source-map`, repreneurs Mozilla sans autre paquet populaire) ; aujourd'hui 4 -> 1 (`path-to-regexp` et `http-errors`, dépendances d'Express, ne sont plus signalés). Rejeu event-stream toujours détecté. Recherche npm limitée (~10 requêtes puis 429) : cache par publieur 6 h en mémoire, échec -> événement laissé signalé avec `publisher_check: unavailable`. Vérifié en prod (la recherche fonctionne depuis Cloudflare). Test : `tests/maintainer-change/top-npm.mjs`.
- [x] Export RapidAPI `presend-openapi.json` resynchronisé le 26 sept. pour `/maintainer-change-check` et `/typosquat-check` (copie depuis openapi.json, GET + POST). Découvert au passage : la description publique de maintainer-change-check sur RapidAPI citait encore "event-stream, ua-parser-js, colors.js" comme cas détectés (leçon n°9, oubliée lors de la correction des 11 publications du 25 sept.), et les deux modes lot POST n'étaient pas publiés sur RapidAPI.
- [ ] **Réimporter la fiche RapidAPI "Presend API"** (export resynchronisé ci-dessus) et vérifier que les deux POST lot ont gardé leur corps JSON.
- [ ] Relire la présentation longue de la fiche RapidAPI "Presend API" (texte saisi dans le dashboard, hors fichiers) : chercher "ua-parser-js" / "colors.js".
- [x] Rate limit : compteurs GET/POST séparés le 26 sept. pour maintainer-change-check ET typosquat-check (`*-batch`), vérifié en local (GET épuisé -> POST toujours accepté). Contexte : GET et POST partageaient le même compteur ; avec l'échantillonnage du GET (+5 une fois sur 5), deux GET peuvent bloquer un POST dans la même minute (constaté en local). Séparer les compteurs.
- [ ] Dette : la règle CI de maintainer-change-check se fie au NOM du compte (`CI_PUBLISHER_RE`), n'importe qui peut s'appeler `foo-bot`. Piste : se fier plutôt à la provenance/trusted publishing (`_npmUser` = GitHub Actions + attestation) quand elle existe.
- [x] Réimport RapidAPI de la fiche "Presend API" fait le 26 sept. (schéma typosquat-check à jour).
- [ ] Vérifier dans le dashboard Cloudflare si le Browser Integrity Check (erreur 1010 sur `Python-urllib`) est réglable pour `presend.pages.dev` (leçon n°12).
- [x] Réimport RapidAPI des 2 fiches fait le 26 sept. (description du paramètre `address` à jour).
- [ ] Glama se recalcule automatiquement : vérifier la nouvelle note (TDQS était C 2,4) à la prochaine session.
- [x] `session-check.sh` réécrit : section "Conversations suivies" alimentée par `scripts/watch.txt` (compteur de référence par conversation, NOUVEAU si dépassé, auteur du dernier commentaire), recherche étendue aux PR (75 cibles : 49 issues + 26 PR, jusque-là invisibles), plafond de 10 lignes supprimé. **En fin de session : mettre à jour les compteurs dans `scripts/watch.txt`.**
- [x] Minnow #1222 : Henri a répondu (demande Discord) ; faille retrouvée dans le code actuel, gravité revue à faible ; autres canaux proposés (voir SECURITY_DISCLOSURES.md).

## Conversations GitHub actives — à suivre, ne pas re-pitcher
- **Compteurs de commentaires de référence : désormais dans `scripts/watch.txt` (source de vérité, vérifiée par session-check.sh). Pour mémoire au 26 sept.** -- tout chiffre supérieur dans `session-check.sh` = nouvelle réponse à lire : tripwire #143 = 2 (mainteneur + notre point d'étape), guard-core #119 = 1 (bot d'accueil, rien à faire), fastapi-guard #137 = 5 (dernier = notre renvoi vers guard-core #119), awesome-mcp-servers #14651 : dernier commentaire = le nôtre (compteur 4 ; le « 40 » noté le 25 sept. était une erreur), IntelOwl #4014 = 8, IntelOwl PR #4037 = 2 (dernier = notre commentaire du 26 sept.).
- **rennf93/fastapi-guard #137 -> rennf93/guard-core #119** : note de conception publiée le 25 sept. (https://github.com/rennf93/guard-core/issues/119) + commentaire de suivi sur #137. **Changement de cap assumé** : après lecture du code, le design ne passe PAS par l'API Presend -- check natif `ThreatListCheck` + `ThreatListRefreshCheck` qui télécharge `drop_v4.json`/`drop_v6.json` directement chez Spamhaus (1 710 + 91 CIDR, ~110 Ko), comparaison locale, calqué sur la paire cloud (`CloudIpRefreshCheck`/`CloudProviderCheck`). Config `block_threat_lists` (frozenset, opt-in) + `threat_list_refresh_interval` (défaut 86400, min 3600). État dans un singleton handler (rien dans le protocole middleware), Redis optionnel pour un seul téléchargement par déploiement, fail-open, sync généré par `make sync`. **4 questions en attente** (forme de config, décorateur par route, type d'événement, liste périmée). On s'est proposés pour écrire le PR. Clone lecture seule : `github-contributions/guard-core`.
- **bunkerity/bunkerweb #3941** : mainteneur dit que la demande revient souvent, va "brainstormer" en interne. Rien à faire de notre côté, attendre leur retour.
- **intelowlproject/IntelOwl #4014 -> PR #4037** : `AnshSinghal` a ouvert le PR `Presend_AddressRisk` (#4037 ; #4035 fermée en doublon), review demandée à mlodic le 25 sept. Code relu le 26 sept. : regex EVM en amont, URL de prod correcte, mock identique à la prod ; seul manque : pas de timeout (suggestion postée). Rien d'autre à faire sauf répondre si questions.
- **neomatrix369/tripwire #143** : feu vert du mainteneur. Fork `presendapp/tripwire` cloné dans `github-contributions/tripwire`. Le 25 sept., commentaire posté (https://github.com/neomatrix369/tripwire/issues/143#issuecomment-5838814495) : `vulnerability-check` retiré (doublon de DepShield), corrections qualité annoncées, et **4 questions en attente de réponse** : (1) OK pour un 1er adaptateur HTTP malgré ADR-0005 (sous-processus CLI) ? (2) budget temps : DEPSHIELD 110 + CARGO_AUDIT 40 + OSSPREY 90 = SCAN_TIMEOUT 240 déjà saturé, où prendre ~20 s (proposé : OSSPREY 90->70) ? (3) actif par défaut ou opt-in (confidentialité des noms de paquets) ? (4) slice stub n°78 vague R ou le mainteneur l'insère lui-même ? **Ne pas coder avant sa réponse.** Design prévu : `run_presend` + `_run_presend_group` en fin de `SCANNER_GROUPS` (`applies_to: both`), réutilise `_find_manifests`, findings `amber` uniquement, erreurs -> `unreachable`, pas de manifeste -> `not_applicable`, `urllib` stdlib **avec un `User-Agent` explicite (sinon 403 Cloudflare 1010, leçon n°12)**, POST batch Presend, maintainer plafonné à 100 paquets (troncature signalée), addendum ADR-0017 + `scanner-output-adapters.md` + DECISIONS, **aucune mention README (règle trust-strip)**, couverture ≥ 95 %, `check-scanner-timeout-budget.sh` à mettre à jour. `partial-failed` est traité comme terminé par `guard/status.py` : une panne Presend ne bloque pas les agents.

## Démarchage GitHub : 49 cibles contactées (vérifié par session-check.sh le 25 sept. au soir, guard-core #119 inclus) depuis le 21 sept., objectif 100-200 sur plusieurs semaines
- Méthode éprouvée : `gh api "search/code?q=<CLE_API_CONCURRENT>+in:file"`, filtrer par étoiles/activité, vérifier l'usage exact avant de pitcher, ne jamais deviner le code sans voir l'interface réelle.
- **Leçon appliquée depuis le 24 sept.** : privilégier `maintainer-change-check`/`typosquat-check`/`address-risk`/`tx-decode`/`supply-chain-check` (vrais différenciateurs) plutôt que `ip-reputation`/`url-reputation`/`malware-check` (souvent redondants chez les outils de sécurité matures).
- Diversifier les termes de recherche à chaque session plutôt que d'épuiser un même filon (plusieurs lots faibles observés quand on insiste trop sur une même piste).

## Divulgations de sécurité en attente (volet recherche séparé, voir historique complet dans les transcripts de session si besoin de détail)
- RunOnFlux/flux : 3 findings envoyés (TOCTOU, SSRF repotag, sessions n'expirant jamais), aucune réponse confirmée
- qrypt.chat, GitLab Runner (namespace overwrite), Next.js/Vercel (VDP, pas de bounty payant) : envoyés, sans réponse confirmée
- HenriGrimm/Minnow #1222 : le mainteneur a répondu le 26 sept. (Discord) ; autres canaux proposés, en attente de son choix. Gravité revue à faible
- Countly/countly-server PR #8041 : contribution de code réelle, en attente de review mainteneur (dernière activité 16 sept.)
- unjs/ipx PR #336 : fermée sans merge le 25 sept. par le mainteneur (voir SECURITY_DISCLOSURES.md)
- Cosmos SDK (Immunefi) : audit x/bank et x/authz commencé, interrompu, pas repris depuis

## Fait aujourd'hui (25 sept., suite)
- [x] punkpeye/awesome-mcp-servers #14651 : badge de score Glama ajouté + compteur d'outils corrige (36 -> 41), mainteneur informe
- [x] Fiche Glama Connectors revendiquee (verification GitHub) : https://glama.ai/mcp/connectors/io.github.presendapp/presend-mcp

## Fait aujourd'hui (25 sept., soir) -- qualité supply-chain + tripwire
- [x] **typosquat-check** : 12 paquets npm très populaires sur 25 testés sortaient "suspects" (ms, qs à 1 édition de ws...). Seuil désormais proportionnel à la longueur (<=3 car. : pas de comparaison approx. ; 4-7 : 1 édition ; 8+ : 2). Résultat : 0 faux positif / 35 légitimes, 17/18 typosquats détectés (seul raté : electorn, car electron absent de la liste).
- [x] **maintainer-change-check** : 17 paquets sains sur 22 sortaient "suspects" (passations légitimes parfois de 2013). Désormais : seuls les événements des 365 derniers jours ; publieurs CI (GitHub Actions, *-bot...) et pré-versions rapportés à part sans lever le drapeau. Résultat : 0/25 suspects aujourd'hui, event-stream toujours détecté en rejouant au 2018-11-26. Timeout (8 s) couvre maintenant le corps de la réponse (typescript = 15,7 Mo).
- [x] **Mode batch POST** sur les deux endpoints (typosquat max 100, maintainer max 20 avec 6 fetchs simultanés), 1 unité de rate limit par lot. Validé en prod : 20 paquets lourds en 1,6 s sans erreur.
- [x] openapi.json + openapi-security-only.json (POST, descriptions, exemple lodash qui montrait à tort `suspicious: true`), README, commentaire de code corrigés.
- [x] Affirmation "event-stream, ua-parser-js, colors.js" corrigée dans **11 issues/PR** (note d'édition datée visible) + ligne du fichier dans la PR bureado #111. Au passage, 3 issues disaient encore maintainer-change-check "npm/PyPI" ou "crates.io-adjacent" (SkillSpector #602, devguard #3083, typomania #36) : corrigé.

## Piste d'amelioration produit identifiee (retour Glama TDQS, score C 2.6/5.0)
- [x] **25 sept. -- qualité MCP, 1re passe (non cassante hormis `ip`)** : 40 descriptions réécrites (moyenne 197 caractères au lieu de ~38 : rôle, quand préférer un outil voisin, limites), vérifiées sur des réponses réelles de l'API ; `ip` retiré (résultat faux via MCP) ; `phone_verify` ne devine plus le pays ; `user_agent` exige `ua` ; serveur v3.0.0. NON fait : renommage/regroupement des outils (cassant pour les intégrations existantes, à décider). Suites : relancer l'analyse Glama (TDQS était C 2,6/5) ; ~~compteur awesome-mcp-servers #14651~~ fait (fichier 41->40, description 36->40, commentaire au mainteneur) ; vérifier si la fiche du registre MCP officiel affiche un nombre d'outils.
Retour independant sur la qualite des definitions d'outils MCP, pas urgent mais a garder en tete :
- Chevauchements ambigus pour un agent IA : email_validate vs email_verify, cve_lookup vs vulnerability_check, verifications individuelles vs versions combinees (security_scan, supply_chain_check)
- Incoherence de nommage : suffixes varies (check/lookup/validate/verify/scan), noms a un mot (base64, color, ip, password, uuid) qui rompent le motif
- 41 outils = trop charge pour un agent, utilitaires generiques melanges aux outils securite
- Piste : envisager de scinder le serveur MCP par domaine (comme les 4 fiches RapidAPI), ou renommer pour une convention plus coherente

## Prochaines étapes suggérées
- [x] **ip-reputation conforme aux conditions Spamhaus (25 sept.)** : copie globale dans KV (`spamhaus-drop:v4`) rafraîchie au plus toutes les 12 h, verrou KV d'1 h (`spamhaus-drop:v4:lock`) pour qu'un seul datacenter télécharge, cache mémoire 5 min, JSON au lieu de drop.txt, `list_date`/`attribution`/`terms`/`stale` dans la réponse, dernière copie servie si Spamhaus est injoignable (stale après 36 h). Testé (12/12, faux KV + fausse horloge, sans téléchargement réel) et vérifié en prod (clés présentes dans KV). Nom Spamhaus retiré des titres/descriptions d'openapi-security-only.json et des exports RapidAPI.
- [x] Réimport RapidAPI des fiches "Presend API" et "Blockchain & Crypto Compliance" (25 sept., fait à la main). A contrôler à l'occasion : les endpoints POST ont-ils gardé leurs corps JSON (bug RapidAPI déjà rencontré à l'import) ?
- **Glama (TDQS C 2,4, calculé à 21 h 06 pendant le déploiement, sur 41 outils)** : se recalcule automatiquement (ou bouton "Sync Server", onglet Admin). Leviers restants : (1) ~~annotations MCP~~ fait le 26 sept. (v3.1.0) ; (2) renforcer les descriptions les plus faibles (favicon B 3,2, password_check, repo_health_check, csv_json, security_headers : manque de "quand l'utiliser") -- la note serveur pèse 40 % sur l'outil le plus faible ; (3) cohérence (nommage 2/5, nombre d'outils 2/5) = structurel, lié au renommage non fait.
- [x] Registre MCP officiel republié en 3.1.0 le 26 sept., description sans nombre d'outils (voir PROJECT_CONTEXT.md).
- Sans urgence : vérifier la version GitHub Packages (`gh auth refresh -s read:packages`) et le plan/volume KV Cloudflare (script /tmp/cf.py de la session du 25 sept., à recréer).
- [x] ip-reputation IPv6 (25 sept.) : `drop_v6.json` avec sa propre copie KV + verrou (`spamhaus-drop:v6`), chargement paresseux, IPv4 encapsulée (::ffff:a.b.c.d) vérifiée contre la liste v4, parseur IPv6 testé sur 23 cas limites. Docs OpenAPI + description MCP + export RapidAPI mis à jour (réimport RapidAPI toujours à faire à la main, voir plus haut).
- **tripwire #143** : attendre la réponse aux 4 questions, puis coder l'adaptateur (voir design ci-dessus)
- **guard-core #119** : attendre la réponse du mainteneur aux 4 questions, puis forker et écrire le PR
- [x] Client npm `presend-api` v1.11.0 publié (25 sept.) : 9 endpoints qui manquaient au client (dont nos différenciateurs maintainer-change, address-risk, tx-decode, supply-chain) + 2 méthodes batch avec découpage automatique, README corrigé (seuil typosquat, IPv6, nom Spamhaus retiré, rate limits 10-60/min), 54/54 tests contre la prod.
- [x] Collection Postman resynchronisée (25 sept.) : 52 requêtes (+ POST batch typosquat/maintainer), descriptions à jour, relue depuis l'API Postman. Nouveau script `scripts/postman-sync.sh` pour les prochaines fois. GitHub Packages `@presendapp/presend-api` : statut inconnu (403, droit `read:packages` manquant) -- vérifier sur https://github.com/presendapp?tab=packages puis décider de la maintenir ou de la retirer de la doc.
- [x] `tx-decode` : vraie transaction Cosmos Hub (bloc 33122643, 3 MsgUndelegate) comme exemple dans openapi.json + exports RapidAPI ; test du client npm activé. Pas encore dans Postman (au prochain `scripts/postman-sync.sh`).
- [x] Lint Redocly réparé (25 sept.) : `"security": []` à la racine (0 erreur au lieu de 52) + 9 exemples/schémas incohérents corrigés (champs `url`, `note`, `wildcard_rules_note` absents du schéma, `null` non déclarés, exemples de paramètres en texte). Reste : `operationId` manquants (52) et réponses 4xx non documentées (39) -- vérifier l'effet d'un `operationId` sur le réimport RapidAPI avant de les ajouter.
- [x] Audit rate limit (25 sept.) : 48 endpoints publics cohérents seuil/message sauf `url-clean` (seuil 30, message 60) -> seuil relevé à 60. Mécanisme commun : écriture KV échantillonnée 1/5 avec +5, donc limite effective = seuil `count >= N`.
- [x] **Quota d'écritures KV (25 sept.)** : `track.js` (appelé au chargement de 336 pages) faisait 2 écritures KV par page vue sans échantillonnage -- plan gratuit = 1 000 écritures/jour, espace KV partagé avec les rate limits de toute l'API (quota épuisé = rate limits désactivés en silence). Désormais échantillonné (rate limit 1/5 +5, visites 1/10 +10) : 303 écritures pour 1 000 pages vues simulées au lieu de 2 000, estimation des visites ~4 % d'erreur, KV en panne -> 200. Reste à vérifier dans le dashboard Cloudflare (Workers & Pages -> KV) le volume réel d'écritures et le plan du compte ; piste plus robuste à terme : Workers Analytics Engine pour les compteurs de visites.
- [x] typosquat-check : liste enrichie et bruit supprimé le 26 sept. (voir "Fait le 26 sept.")
- Continuer le démarchage (nouveaux termes de recherche à chaque session)
- Ouvrir la note de conception guard-core pour fastapi-guard
- Envisager d'étendre address-risk aux adresses Cosmos SDK (bech32) — cohérent avec le reste du catalogue, déjà demandé indirectement 2 fois
- Revoir la question des paliers payants RapidAPI une fois qu'il y a de vraies données d'usage
